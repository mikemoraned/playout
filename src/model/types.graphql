type Query {
  current_user: User!
  recently_completed(limit: Int!): [ProblemSpec]
}

type User {
  suggestions: [GameLink]
  next: ProblemSpec
}

type ProblemSpec {
  gridSpec: String!
  teamsSpec: String!
}

type ProblemLink {
  path: String!
  name: String!
}

type Mutation {
  problemCompleted(problemSpec: ProblemSpec): ProblemSpec!
}
